export enum TransportDataFilesNames {
    AGENCY = 'agency',
    ROUTES = 'routes',
    STOP_TIMES = 'stop_times',
    STOPS = 'stops',
    TRIPS = 'trips',
    VEHICLE_TYPES = 'vehicle_types',
    ROUTE_TYPES = 'route_types',
}

export enum MapLayers {
    WROCLAW_TRANSPORT = 'wroclawTransport',
}